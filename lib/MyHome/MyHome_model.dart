import 'package:equatable/equatable.dart';

/// generate by https://javiercbk.github.io/json_to_dart/
class AutogeneratedMyHome {
  List<MyHomeModel> results;

  AutogeneratedMyHome({this.results});

  AutogeneratedMyHome.fromJson(Map<String, dynamic> json) {
    if (json['results'] != null) {
      results = new List<MyHomeModel>();
      json['results'].forEach((v) {
        results.add(new MyHomeModel.fromJson(v));
      });
    }
  }

  Map<String, dynamic> toJson() {
    final Map<String, dynamic> data = new Map<String, dynamic>();
    if (this.results != null) {
      data['results'] = this.results.map((v) => v.toJson()).toList();
    }
    return data;
  }
}

class MyHomeModel extends Equatable {
  int id;
  String name;

  MyHomeModel({this.id, this.name}) : super([id, name]);

  /// use : super([json]) for Equatable
  MyHomeModel.fromJson(Map<String, dynamic> json) : super([json]) {
    id = json['id'];
    name = json['name'];
  }

  Map<String, dynamic> toJson() {
    final Map<String, dynamic> data = new Map<String, dynamic>();
    data['id'] = this.id;
    data['name'] = this.name;
    return data;
  }
}
